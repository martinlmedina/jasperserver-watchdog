# Normalize all text to LF in the repo and on checkout. These are shell
# scripts, systemd units, SQL and Markdown deployed to Linux hosts; CRLF
# would break the scripts (e.g. `set -euo pipefail\r`). eol=lf overrides a
# contributor's core.autocrlf on Windows.
* text=auto eol=lf
