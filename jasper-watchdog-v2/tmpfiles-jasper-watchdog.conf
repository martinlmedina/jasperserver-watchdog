# Keep watchdog incident directories for 90 days. systemd-tmpfiles will clean
# aged content according to the host cleanup schedule.
d /var/log/jasper-watchdog 0750 root root -
d /var/log/jasper-watchdog/incidents 0700 root root 90d
